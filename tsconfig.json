{
  "compilerOptions": {
    "target": "es6",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    /* type cheking */
    "strict": true,
    "noImplicitAny": true,   // 型がanyに推論される時はコンパイルエラー
    "noImplicitThis": true,  // thisの型を明示させる。
    "alwaysStrict": true,    // 各ファイルをECMAScriptのstrictモードで解釈。
    "strictNullChecks": true, // 非Null型へのnullの代入、非undefined型へのundefinedの代入をコンパイルエラー
    "strictPropertyInitialization": true, // クラスの変数が初期化されていない場合コンパイルエラー
    "strictBindCallApply": true,  // bind、call、applyで呼び出す関数の引数の型チェックを行う。
    "strictFunctionTypes": true,  // 関数の代入時、引数型の範囲を狭める代入を許さない（引数の共変性を無くす）
    "useUnknownInCatchVariables": true,   // 例外が捕捉した値を全てunknown型として解釈する。
    /* ここまでstrict: trueでonになるもの */
    /* type cheking end */
    "forceConsistentCasingInFileNames": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "node",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true,
    "baseUrl": "src"
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx"],
  "exclude": ["node_modules"]
}
